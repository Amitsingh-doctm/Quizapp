import React, { useState } from 'react'


export default function List() {
    let s = [

    ]



    const [h, seth] = useState(s);
   // setmyt([2])
   function dd()
   {
   seth(5)
   }
    /*console.log(m);

       
    function ff()
    {1
        if(cc(s,m))
        {
        setmym(dd());
        }
        else{
            setmym([4,6,8]);
        }

    }

    function dd()
    {
        let arr=[];
        for(let i=0;i<s.length;i++)
        {
             arr.push(s[i]+1);
        }
        return arr;
    }
    function cc(s,m)
    {
        let d=true;
        for(let i=0;i<s.length;i++)
        {
            if(s[i]!=m[i])
            {
               d=false;
            }
        }
     return d;
    }
    
    function as()
    {
      let d=  m.map((curElm) => {
         
         return   <p> {curElm}</p>;
        })
        return d;
    }
           
     console.log(s)
     */

    return (
        <div>
            <button> dd</button>
            <p onClick={dd}> dvsbdf</p>
        </div>
    )
}
